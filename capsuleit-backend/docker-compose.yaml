version: "3.8"

services:
  sql-db:
    image: postgres
    container_name: postgres-dev
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: devDB
    volumes:
      - postgres:/var/lib/postgresql

volumes:
  postgres:
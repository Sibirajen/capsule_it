server:
  port: 8080

spring:
  # --- PostgreSQL ---
  datasource:
    url: jdbc:postgresql://localhost:5432/devDB
    username: admin
    password: password123
    driver-class-name: org.postgresql.Driver